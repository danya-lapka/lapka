{
  "name": "danya-lapka",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo check-types build",
    "publish:css-vite": "npm run build && cd packages/css-vite && npm publish --access public",
    "publish:components-vue": "npm run build && cd packages/components-vue && npm publish --access public"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  },
  "packageManager": "npm@11.6.0"
}
