{
  "name": "danya-lapka",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo check-types build",
    "publish:css": "cd packages/css && npm run build && npm publish --access public",
    "publish:components-vue": "cd packages/components-vue && npm run build && npm publish --access public",
    "sync": "powershell -ExecutionPolicy Bypass -File sync-packages.ps1",
    "sync:css": "powershell -ExecutionPolicy Bypass -File sync-packages.ps1 css",
    "sync:vue": "powershell -ExecutionPolicy Bypass -File sync-packages.ps1 components-vue"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
